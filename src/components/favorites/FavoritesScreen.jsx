import React from 'react'

import { FavoritesList } from '../favorites/FavoritesList';

export const FavoritesScreen = () => {
    return (
        <FavoritesList title="Productos favoritos"/>
      );
}
